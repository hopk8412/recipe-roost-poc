global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'recipe-roost'
    static_configs:
      # 'app' is the Docker Compose service name; port 3000 is the Node server.
      - targets: ['app:3000']
    metrics_path: /api/metrics
